<block wx:if="{{ !isLoad }}">
  <loading></loading>
</block>
<block wx:else>
  <block wx:if="{{ isAuth }}">
    <view class="ad-box">
    </view>
    <button bindtap="viewAd">查看广告</button>
  </block>
  <block wx:else>
    <auditPage></auditPage>
  </block>
</block>
